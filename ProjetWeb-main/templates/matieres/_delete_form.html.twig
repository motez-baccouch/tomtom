<form method="post" action="{{ path('matieres_delete', {'id': matiere_niveau_filiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matiere_niveau_filiere.id) }}">
    <button class="btn btn-danger form-btn">Delete</button>
</form>
