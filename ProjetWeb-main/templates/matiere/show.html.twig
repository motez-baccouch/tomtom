{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary form-btn" href="{{ path('matiere_edit', {'id': matiere.id}) }}">edit</a>
    <br>
    <a class="btn btn-secondary form-btn" href="{{ path('matiere_index') }}">back to list</a>

    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
