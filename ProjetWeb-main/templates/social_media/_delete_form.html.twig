<form method="post" action="{{ path('social_media_delete', {'id': social_media.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social_media.id) }}">
    <button class="btn btn-danger form-btn">Delete</button>
</form>
