{% extends 'base.html.twig' %}

{% block body %}

    {{ include('etudiant/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary form-btn" href="{{ path('etudiant_index') }}">back to list</a>

    {{ include('etudiant/_delete_form.html.twig') }}
{% endblock %}
