{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emploi_du_temp.id }}</td>
            </tr>
            <tr>
                <th>AnneeScolaire</th>
                <td>{{ emploi_du_temp.anneeScolaire }}</td>
            </tr>
            <tr>
                <th>Filière</th>
                <td>{{ emploi_du_temp.filiere.filiere}}</td>
            </tr>
            <tr>
                <th>Semestre</th>
                <td>{{ emploi_du_temp.semestre}}</td>
            </tr>
            <tr>
                <th>Document</th>
                <td>
                    {% if emploi_du_temp.doc is defined and emploi_du_temp.doc %}
                        <a target="_blank" href="{{ asset(emploi_du_temp.doc.getFullUrl()) }}">
                            <i class="x4"><iconify-icon data-icon="fluent:document-pdf-32-filled"></iconify-icon></i>
                        </a>
                    {% endif %}
                </td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary form-btn" href="{{ path('emploi_du_temps_edit', {'id': emploi_du_temp.id}) }}">edit</a>
    <br>
    <a class="btn btn-secondary form-btn" href="{{ path('emploi_du_temps_index') }}">back to list</a>

    {{ include('emploi_du_temps/_delete_form.html.twig') }}
{% endblock %}
