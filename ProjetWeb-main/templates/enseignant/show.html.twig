{% extends 'table.html.twig' %}


{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enseignant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enseignant.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ enseignant.email }}</td>
            </tr>
            <tr>
                <th>Departement</th>
                <td>{{ enseignant.departement.getNom }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-primary form-btn"  href="{{ path('enseignant_edit', {'id': enseignant.id}) }}">edit</a>
    <br>
    <a class="btn btn-secondary form-btn"  href="{{ path('enseignant_index') }}">back to list</a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
