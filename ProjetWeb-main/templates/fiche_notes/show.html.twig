{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_note.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ fiche_note.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_notes_index') }}">back to list</a>

    <a href="{{ path('fiche_notes_edit', {'id': fiche_note.id}) }}">edit</a>

    {{ include('fiche_notes/_delete_form.html.twig') }}
{% endblock %}
