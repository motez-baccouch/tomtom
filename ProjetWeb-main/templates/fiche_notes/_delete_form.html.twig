<form method="post" action="{{ path('fiche_notes_delete', {'id': fiche_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_note.id) }}">
    <button class="btn">Delete</button>
</form>
