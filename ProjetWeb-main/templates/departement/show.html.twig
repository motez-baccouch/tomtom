{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ departement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary form-btn" href="{{ path('departement_edit', {'id': departement.id}) }}">edit</a>
    <br>
    <a class="btn btn-secondary form-btn" href="{{ path('departement_index') }}">back to list</a>

    {{ include('departement/_delete_form.html.twig') }}
{% endblock %}
