{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filiere.id }}</td>
            </tr>
            <tr>
                <th>Filiere</th>
                <td>{{ filiere.filiere }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ filiere.ordre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary form-btn" href="{{ path('filiere_edit', {'id': filiere.id}) }}">edit</a>
    <br>
    <a class="btn btn-secondary form-btn" href="{{ path('filiere_index') }}">back to list</a>


    {{ include('filiere/_delete_form.html.twig') }}
{% endblock %}
