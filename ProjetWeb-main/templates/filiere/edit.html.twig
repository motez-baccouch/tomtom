{% extends 'base.html.twig' %}

{% block body %}

    {{ include('filiere/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary form-btn" href="{{ path('filiere_index') }}">back to list</a>

    {{ include('filiere/_delete_form.html.twig') }}
{% endblock %}
